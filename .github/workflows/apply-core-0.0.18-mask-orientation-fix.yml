name: Apply Core 0.0.23 Safe Minimap Draw

on:
  push:
    branches: [mod-prototype]
    paths:
      - '.github/workflows/apply-core-0.0.18-mask-orientation-fix.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: mod-prototype
          fetch-depth: 0

      - name: Replace unsafe minimap texture drawing
        run: python dev-tools/apply_core_0_0_23_safe_minimap_draw.py

      - name: Commit safe minimap rendering fix
        run: |
          if git diff --quiet; then
            echo 'No changes produced.'
            exit 1
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add in-game-mod/core/CoreEntry.cs in-game-mod/core/BigWalkHideSeek.Core.csproj
          git commit -m 'Avoid unsafe minimap texture coordinates on IL2CPP'
          git push origin HEAD:mod-prototype
